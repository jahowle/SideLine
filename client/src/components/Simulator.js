import React, { useState } from "react";

function Simulator({ plays }) {
  const [simulate, setSimulate] = useState(false);

  return (
    <div className="w-100% flex flex-col items-center h-12 bg-slate-950">
      <h1 className="">Simulator</h1>
    </div>
  );
}

export default Simulator;
